## 开发流程：先描述，后实现

每个模块严格遵循 **设计文档 → 用户审阅 → Verilog 代码** 的流程，
**两步之间必须等待用户确认，绝不能在同一轮对话中连续执行。**

```
1. 写 .md 设计描述 → 停止，等待用户审阅
2. 用户确认 .md 无误后 → 编写 .v 实现代码
3. 用 verilator --lint-only 检查语法
```

**核心约束：同一轮对话中，完成步骤 1 后必须停止，不得继续执行步骤 2。**
用户明确说"可以开始写 .v"、"通过"、"没问题"等才算确认。

### 目录对应关系

设计文档与源码目录结构镜像：

| 设计文档 | 源码 |
|----------|------|
| `design_doc/rtl/stage/<stage>/<module>.md` | `src/rtl/stage/<stage>/<module>.v` |
| `design_doc/rtl/stage/<stage>/<stage>_stage.md` | (整体阶段描述，无对应 .v) |

### 设计文档内容要素

每个 `<module>.md` 必须覆盖：

1. **概述** — 模块职责、在流水线中的位置
2. **端口定义** — 输入/输出表，含信号名、宽度、来源/去向、说明
3. **功能描述** — 核心行为（逻辑公式、优先级、边界条件）
4. **实现要点** — 组合/时序、复位策略、综合注意事项
5. **接口时序** — 关键路径，与其他模块的交互
6. **未来扩展** — 预留接口和待完善项

### 代码实现时的约束

- 严格按照设计文档的端口和功能编写 .v
- **禁止在 .v 中添加 .md 未定义的端口、信号或功能。**
  一旦发现需求上有缺失，先回退修改 .md，用户确认后再改 .v。
- 发现设计问题时**先改 .md 再改 .v**，保持文档与代码一致
- 涉及新常量时，同步更新 `const_define.vh`

# 编码规范

## 头文件引用

- 项目全局常量统一定义在 `const_define.vh`，通过 `` `include "const_define.vh" `` 引用。
- **不写相对路径**，直接写文件名。Vivado 工程中会配置 include 搜索路径（`-I`）。
- 仿真时手动指定：`verilator --lint-only -Isrc/rtl/defines <file.v>`。
- 头文件使用 `` `ifndef / `define / `endif `` 防止重复包含。

## 常量命名

- **所有数值字面量（常数）必须用 `` `define `` 宏，模块内禁止出现任何裸数字（包括 `32'h0`、`32'd4` 等）。**
  这条规则没有例外——只要不是 `localparam` 或变量，就必须是宏。
- 宏名全大写 + 下划线分隔，如 `` `INST_MEM_DEPTH ``、`` `PC_INCREMENT ``。
- 宽度相关的派生值在模块内用 `localparam` 从宏计算，如 `$clog2(DEPTH)`。

## 模块编写

### 文件头注释

每个 `.v` 文件顶部写：

```verilog
//=============================================================================
// <模块名> — <一句话职责>
//=============================================================================
// <关键行为或限制说明>
//=============================================================================
```

### 端口命名

| 方向 | 前缀 | 示例 |
|------|------|------|
| input | `i_` | `i_pc`, `i_stall` |
| output | `o_` | `o_pc_next`, `o_instruction` |

- 端口列表每行一个信号，对齐注释说明来源/去向。

### 模块内组织顺序

1. 参数定义（`localparam`）
2. 寄存器／存储器声明
3. `initial` 初始化块
4. `assign` 连续赋值
5. `always_comb` / `always_ff` 块

### 组合逻辑

- 优先用 `assign`，复杂选择用 `? :` 链。
- 需要多条语句时用 `always_comb`，确保所有分支完整覆盖（避免锁存器）。

### 常量处理

- 地址索引从宏派生的 `localparam` 计算，不写死 bit 范围。
  例：`localparam ADDR_HI = ADDR_LO + $clog2(DEPTH) - 1;`
- 使模块可随 `const_define.vh` 中宏的修改自动适配。

## 仿真与综合

- 仿真专用代码用 `` `ifdef VERILATOR `` / `` `ifdef SIMULATION `` 包裹。
- Xilinx 综合属性用注释保留，需要时取消注释：
  ```verilog
  // (* ram_style = "block" *)
  ```
